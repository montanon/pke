// HLAM-128 — minimal stub for Linux CI bisect.

import Foundation

public actor WitnessDispatcher {
    public init(window: TimeInterval = 30, cap: Int = 50) {
        _ = window
        _ = cap
    }
}
